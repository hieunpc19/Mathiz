import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ParentDashboardPage() {
  return (
    <RoutePlaceholder
      title="Tổng quan gia đình"
      description="Khu vực này sau này sẽ giúp phụ huynh theo dõi hoạt động học tập mà không hiển thị số liệu giả ở giai đoạn nền tảng."
      route="/parent/dashboard"
    />
  );
}
