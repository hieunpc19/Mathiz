import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ParentExamsPage() {
  return (
    <RoutePlaceholder
      title="Quản lý đề thi"
      description="Trang này sau này sẽ liệt kê đề, phiên bản và trạng thái xuất bản trong gia đình."
      route="/parent/exams"
    />
  );
}
