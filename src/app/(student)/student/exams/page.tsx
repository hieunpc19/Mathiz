import { RoutePlaceholder } from "@/components/route-placeholder";

export default function StudentExamsPage() {
  return (
    <RoutePlaceholder
      title="Đề thi dành cho bé"
      description="Khu vực này sau này sẽ hiển thị các đề đã được phụ huynh xuất bản và phù hợp với hồ sơ của bé."
      route="/student/exams"
    />
  );
}
